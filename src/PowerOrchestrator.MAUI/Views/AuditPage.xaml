<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PowerOrchestrator.MAUI.Views.AuditPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:m="clr-namespace:UraniumUI.Icons.MaterialIcons;assembly=UraniumUI.Icons.MaterialIcons"
    xmlns:viewmodels="clr-namespace:PowerOrchestrator.MAUI.ViewModels"
    x:DataType="viewmodels:AuditViewModel"
    Title="{Binding Title}"
    BackgroundColor="{DynamicResource Surface}">

    <StackLayout Padding="20" Spacing="20">
        <material:CardView BackgroundColor="{DynamicResource ErrorContainer}" CornerRadius="12">
            <StackLayout Padding="20" Spacing="10" HorizontalOptions="Center">
                <Label 
                    Text="{x:Static m:MaterialIcons.Assignment}"
                    FontFamily="MaterialRegular"
                    FontSize="48"
                    TextColor="{DynamicResource OnErrorContainer}"
                    HorizontalOptions="Center" />
                <Label 
                    Text="Audit Logs"
                    FontSize="24"
                    FontAttributes="Bold"
                    TextColor="{DynamicResource OnErrorContainer}"
                    HorizontalOptions="Center" />
                <Label 
                    Text="Track system activities and security events"
                    FontSize="16"
                    TextColor="{DynamicResource OnErrorContainer}"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center" />
            </StackLayout>
        </material:CardView>
        
        <Label 
            Text="This page will show audit trails, system events, security logs, and compliance reporting."
            FontSize="14"
            TextColor="{DynamicResource Outline}"
            HorizontalTextAlignment="Center" />
    </StackLayout>

</ContentPage>