<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PowerOrchestrator.MAUI.Views.UsersPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:m="clr-namespace:UraniumUI.Icons.MaterialIcons;assembly=UraniumUI.Icons.MaterialIcons"
    xmlns:viewmodels="clr-namespace:PowerOrchestrator.MAUI.ViewModels"
    x:DataType="viewmodels:UsersViewModel"
    Title="{Binding Title}"
    BackgroundColor="{DynamicResource Surface}">

    <StackLayout Padding="20" Spacing="20">
        <material:CardView BackgroundColor="{DynamicResource SecondaryContainer}" CornerRadius="12">
            <StackLayout Padding="20" Spacing="10" HorizontalOptions="Center">
                <Label 
                    Text="{x:Static m:MaterialIcons.People}"
                    FontFamily="MaterialRegular"
                    FontSize="48"
                    TextColor="{DynamicResource OnSecondaryContainer}"
                    HorizontalOptions="Center" />
                <Label 
                    Text="User Management"
                    FontSize="24"
                    FontAttributes="Bold"
                    TextColor="{DynamicResource OnSecondaryContainer}"
                    HorizontalOptions="Center" />
                <Label 
                    Text="Manage users, permissions, and access control"
                    FontSize="16"
                    TextColor="{DynamicResource OnSecondaryContainer}"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center" />
            </StackLayout>
        </material:CardView>
        
        <Label 
            Text="This page will show user accounts, roles, permissions, and user management features."
            FontSize="14"
            TextColor="{DynamicResource Outline}"
            HorizontalTextAlignment="Center" />
    </StackLayout>

</ContentPage>